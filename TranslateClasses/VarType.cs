namespace TranslateClass;

public enum VarType
{
    // Types of vars in programme
    Int,
    String,
    Float,
    List,
    None, // don't recognize type
    VarName // means, that this string is a var name
}